import React from 'react'
import PropTypes from 'prop-types'
import Modal from '../Modal/Modal'

const OrderOverlay = props => {
  return (
    <Modal>
      <div>OrderOverlay</div>
    </Modal>
  )
}

OrderOverlay.propTypes = {}

export default OrderOverlay